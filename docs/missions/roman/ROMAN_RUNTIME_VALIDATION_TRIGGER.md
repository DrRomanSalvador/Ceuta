# ROMAN runtime validation trigger

This marker intentionally triggers the canonical CI path for the repository-level ROMAN runtime validation tests. It introduces no runtime implementation or duplicate control-plane infrastructure.
