# Classification Prompt — CeutIA

**Status:** REQUIRES VALIDATION (template)

When classifying observations or signals:

- Emit an epistemic state from the closed set.
- Never upgrade a state without new evidence.
- Fail closed on insufficient data.
- Apply epistemic_validation_prompt.md constraints.
